#ifndef	_APP_DISPLAYPORT_H
#define	_APP_DISPLAYPORT_H

#include	"E7602_PG_Application.h"

#define	DP_CHANNEL_MAX_COUNT	2

enum{
	DP_AVAILABLE_TX_1			= 0x00000001,
	DP_AVAILABLE_TX_2			= 0x00000002,

	DP_AVAILABLE_RX_1			= 0x00000400,
	DP_AVAILABLE_RX_2			= 0x00000800,
};

enum{
	DP_CORE_NONE				= 0,
	DP_CORE_1,
	DP_CORE_2,
	DP_CORE_END,
};

enum{
	DP_CHANNEL_NONE				= 0,
	DP_CHANNEL_COMMON_1,
	DP_CHANNEL_COMMON_2,
	DP_CHANNEL_TX1,
	DP_CHANNEL_TX2,
	DP_CHANNEL_RX1,
	DP_CHANNEL_RX2,
	DP_CHANNEL_END,
};

enum{
	DISPLAYPORT_LINK_RATE_RBR		= 6,			//	RBR = 1.62Gbps
	DISPLAYPORT_LINK_RATE_HBR		= 10,			//	HBR = 2.7Gbps
	DISPLAYPORT_LINK_RATE_HBR2		= 20,			//	HBR2 = 5.4Gbps
	DISPLAYPORT_LINK_RATE_HBR3		= 30,			//	HBR3 = 8.1Gbps
};

enum{
	DISPLAYPORT_LANE_COUNT_1		= 1,
	DISPLAYPORT_LANE_COUNT_2		= 2,
	DISPLAYPORT_LANE_COUNT_3		= 3,
	DISPLAYPORT_LANE_COUNT_4		= 4,
};

enum{
	DISPLAYPORT_LINKTRAINING_MODE_NONE		= 0,
	DISPLAYPORT_LINKTRAINING_MODE_AUTO,
	DISPLAYPORT_LINKTRAINING_MODE_NORMAL,
	DISPLAYPORT_LINKTRAINING_MODE_FASE_1,
	DISPLAYPORT_LINKTRAINING_MODE_FASE_2,
	DISPLAYPORT_LINKTRAINING_MODE_QUICK_1,
	DISPLAYPORT_LINKTRAINING_MODE_QUICK_2,
	DISPLAYPORT_LINKTRAINING_MODE_END,
};

enum{
	DISPLAYPORT_COLORSPACE_FORMAT_RGB			= 0,
	DISPLAYPORT_COLORSPACE_FORMAT_YCBCR_444,
	DISPLAYPORT_COLORSPACE_FORMAT_YCBCR_422,
	DISPLAYPORT_COLORSPACE_FORMAT_YCBCR_420,
	DISPLAYPORT_COLORSPACE_FORMAT_END,
};

enum{
	DISPLAYPORT_COLORSPACE_BPC_6BIT				= 0,
	DISPLAYPORT_COLORSPACE_BPC_8BIT,			//	1
	DISPLAYPORT_COLORSPACE_BPC_10BIT,			//	2
	DISPLAYPORT_COLORSPACE_BPC_12BIT,			//	3
	DISPLAYPORT_COLORSPACE_BPC_16BIT,			//	4
	DISPLAYPORT_COLORSPACE_BPC_7BIT,			//	5
	DISPLAYPORT_COLORSPACE_BPC_14BIT,			//	6
	DISPLAYPORT_COLORSPACE_BPC_9BIT,			//	7
	DISPLAYPORT_COLORSPACE_BPC_END,
};


enum{
	DISPLAYPORT_COLORSPACE_RANGE_VESA			= 0,
	DISPLAYPORT_COLORSPACE_RANGE_CEA,
	DISPLAYPORT_COLORSPACE_RANGE_END,
};


enum{
	CORE_REG_BIT_REGION_DP_TX_NONE					= 0x00000000,
	CORE_REG_BIT_REGION_DP_TX_DEFAULT				= 0x00000001,
	CORE_REG_BIT_REGION_DP_TX_LINKTRAINING			= 0x00000002,
	CORE_REG_BIT_REGION_DP_TX_SFUNCTION				= 0x00000004,
	CORE_REG_BIT_REGION_DP_TX_TRANSACTION			= 0x00000008,
};

enum{
	CORE_REG_BIT_REGION_DP_RX_NONE					= 0x00000000,
	DP_RX_PROCESS_FLAG_RESERVED_1					= 0x00000001,
	CORE_REG_BIT_REGION_DP_RX_INITIALIZE			= 0x00000002,
	CORE_REG_BIT_REGION_DP_RX_SFUNCTION				= 0x00000004,
	DP_RX_PROCESS_FLAG_RESERVED_4					= 0x00000008,
};


enum{
	CORE_REG_REGION_COMMON_INITIALIZE				= 0x00000001,
	CORE_REG_REGION_COMMON_VIDEO_CONFIG				= 0x00000002,
	CORE_REG_REGION_COMMON_RESERVE_3				= 0x00000100,
	CORE_REG_REGION_COMMON_CLK						= 0x00000004,
	CORE_REG_REGION_COMMON_RESERVE_4				= 0x00000008,

	CORE_REG_REGION_DP_TX_1							= 0x00000010,
	CORE_REG_REGION_DP_TX_1_LINKTRAINING			= 0x00000020,
	CORE_REG_REGION_DP_TX_1_SFUNCTION				= 0x00000040,
	CORE_REG_REGION_DP_TX_1_TRANSACTION				= 0x00000080,

	//CORE_REG_REGION_DP_TX_2							= 0x00000100,
	CORE_REG_REGION_DP_TX_2_LINKTRAINING			= 0x00000200,
	CORE_REG_REGION_DP_TX_2_SFUNCTION				= 0x00000400,
	CORE_REG_REGION_DP_TX_2_TRANSACTION				= 0x00000800,

	CORE_REG_REGION_DP_TX_3							= 0x00001000,
	CORE_REG_REGION_DP_TX_3_LINKTRAINING			= 0x00002000,
	CORE_REG_REGION_DP_TX_3_SFUNCTION				= 0x00004000,
	CORE_REG_REGION_DP_TX_3_TRANSACTION				= 0x00008000,

	CORE_REG_REGION_DP_TX_4							= 0x00010000,
	CORE_REG_REGION_DP_TX_4_LINKTRAINING			= 0x00020000,
	CORE_REG_REGION_DP_TX_4_SFUNCTION				= 0x00040000,
	CORE_REG_REGION_DP_TX_4_TRANSACTION				= 0x00080000,

	CORE_REG_REGION_DP_TX_5							= 0x00100000,
	CORE_REG_REGION_DP_TX_5_LINKTRAINING			= 0x00200000,
	CORE_REG_REGION_DP_TX_5_SFUNCTION				= 0x00400000,
	CORE_REG_REGION_DP_TX_5_TRANSACTION				= 0x00800000,

	CORE_REG_REGION_DP_RX							= 0x01000000,
	CORE_REG_REGION_DP_RX_INITIALIZE				= 0x02000000,	// 201123_lyh
	CORE_REG_REGION_DP_RX_SFUNCTION					= 0x04000000,

	CORE_REG_REGION_CLEAR							= 0x20000000,
	CORE_REG_REGION_FAIL							= 0x40000000,
	CORE_REG_REGION_BUSY							= 0x80000000,
};


u8 DP_Init();
u32 DP_GetAvailableChannel();
u8 DP_CheckAvailableChannel(u8 position);
u8 DP_SetActivateChannel(u8 position, u8 state);
u8 DP_GetFlagPosition(u8 position, u32 *pBitFlag);
u8 DP_GetChipSelect(u8 position, u8 *pChipSelect);
u8 DP_GetChannelInfo(u8 position, u8 *pChipSelect, u16 *pBaseAdrs);
u8 DP_ProcessStart(u8 chipSelect, u32 mode);
u8 DP_ProcessCheck(u8 chipSelect, u32 delay);
//u8 DP_ProcessCheck_extend(u8 chipSelect, u32 delay);
u8 DP_ProcessCheck_extend(u8 chipSelect);
u8 DP_TxMainLinkConfig(u8 chipSelect, u32 linkRate, u32 laneCount);
u8 DP_TxHpdEventConfig(u8 chipSelect, u32 ltMode, u32 edidReadDisable, u32 videoOutputDisable);
u8 DP_TxColorSpaceConfig(u8 chipSelect, u32 format, u32 bpc, u32 range, u32 useVscSdp, u32 colorimetry);
u8 DP_TxVideoConfigDummyVideo(u8 chipSelect, u32 alpdpEnable, u32 beMode, u32 dummyVFP, u32 dummyVBP);
u8 DP_TxBlankConfig(u8 chipSelect, u32 sof, u32 eof);
u8 DP_TxAuxConfig(u8 chipSelect, u32	 preamble, u32 precharge, u32 INVT);
u8 DP_TxSdpBurstConfig(u8 chipSelect, u32	sdpBurstCtrl, u32 sdpBurstCount, u32 sdpBurstMode, u32 sdpBurstHBlankSLine);
u8 DP_TxAlpmSignal(u8 chipSelect, u32 mainLinkSignal, u32 wakeUpSignal, u32 symbolLockSignal, u32 auxAckCheck);
u8 DP_TxAlpmTiming(u8 chipSelect, u32 timing_T12, u32 timing_N1_vb, u32 timing_N2_vb, u32 timing_N3_vb, u32 timing_N5b_vb);
//u8 DP_TxAlpmClk(u8 chipSelect, u32 lrc_enable, u32 lrc_mode, u32 lrc_frame_period, u32 	rate);
u8 DP_TxAlpmClk(u8 chipSelect, u32 rate);
u8 DP_TxAlpmLrc(u8 chipSelect, u32 lrc_enable, u32 lrc_mode, u32 lrc_frame_period);
u8 DP_TxACVRRCtrl(u8 chipSelect, u32 Range, u32 Enable);
u8 DP_TxACVRRSet(u8 chipSelect, u32 NUM, u32 Hz, u32 CNT);
u8 DP_TxAUXINV(u8 chipSelect, u32 status);
u8 DP_RxInit(u8 chipSelect);	// 201123_lyh
u8 DP_Rx_EDID_CTRL(u8 chipSelect, u16 adrs, u32 flag);	// 201123_lyh
u8 DP_Rx_EDID_WRITE(u8 chipSelect, u16 adrs, u8 *pData, u16 size);	// 201123_lyh
u8 DP_XSYNC(u8 chipSelect, u32 xsync_enable, u32 duty, u32 shift, u32 option);
u8 DP_BSYNC(u8 chipSelect, u32 bsync_enable, u32 width, u32 shift, u32 option);

#endif	// _APP_DISPLAYPORT_H
